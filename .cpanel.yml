---
deployment:
  tasks:
    # 1. Copy the built server and asset files to your production application folder
    - /bin/cp -R app/dist* /home/infinite/repositories/RestaurantSaaSPlatform/app/
    
    # 2. Copy the package files so the production server knows its entry point
    - /bin/cp app/package.json /home/infinite/repositories/RestaurantSaaSPlatform/app/