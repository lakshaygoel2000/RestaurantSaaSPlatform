# ── Backend ─────────────────────────────────────────────────────
APP_ID=                   # Application ID
APP_SECRET=               # Application secret (used for JWT signing)

# ── Database ───────────────────────────────────────────────────
# MySQL connection string. Examples:
#   TiDB Cloud: mysql://user:pass@gateway.xxx.tidbcloud.com:4000/dbname?ssl={"rejectUnauthorized":true}
#   cPanel/MySQL: mysql://user:pass@localhost:3306/dbname
#   cPanel (no SSL): mysql://user:pass@localhost:3306/dbname?ssl=false
DATABASE_URL=

# Optional DB SSL mode override (disabled | required | accept-invalid)
# Set to "disabled" for most cPanel shared-hosting MySQL setups.
DB_SSL_MODE=disabled

# Optional: tune the connection pool size (default 10).
DB_CONNECTION_LIMIT=10

# ── Deployment ──────────────────────────────────────────────────
PORT=3000                 # Port the Node server listens on
# ALLOWED_ORIGIN=         # Production origin for CORS (e.g. https://your-domain.com)
